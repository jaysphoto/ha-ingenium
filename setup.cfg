[tool:pytest]
addopts = -qq
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

[coverage:run]
source = custom_components/ingenium

# Linting tools configuration
[tool:ruff]
line-length = 88
extend-exclude = ["__pycache__"]
